pipeline{
    agent any
    stages{
        stage('verify branch'){
            steps{
                echo "Hello world"
            }
        }
        stage('Docker Build'){
            pwsh(script: 'docker images -a')
            pwsh(script: """
            cd azure-vote/
            docker images -a
            docker build -t jenkins-pipeling .
            docker images -a
            cd ..
            """)
        }
    }
}
